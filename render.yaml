services:
  - type: web
    name: quantum-gate-v8
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn index:app
    envVars:
      - key: PORT
        value: 5000